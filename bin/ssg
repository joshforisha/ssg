#!/usr/bin/env node

if (process.argv.length !== 5) {
  console.log('usage: ssg <source-dir> <base-url> <destination-dir>')
  process.exit(0)
}

const { generate } = require('../lib/utils.js')

generate()
